# The release workflow triggers on a bare `1.2.3` tag, because that is the
# version Obsidian's installer looks for and it has to match manifest.json
# exactly. npm's default prefix would produce `v1.2.3`, which the workflow
# ignores - a release that silently never happens. Keep the two agreeing.
tag-version-prefix=""
